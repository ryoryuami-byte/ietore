# イエトレ 修正メモ（2026-08-16）

公開中のアプリ（Artifact）を実際にブラウザで動かして点検し、見つかった不具合を修正しました。
修正後のアプリ本体は `ietore.html` です（同じ Artifact URL に反映済み）。

修正はビルド済みのバンドルに対する最小限のパッチとして当てています。以下、内容と再現手順です。

---

## 1. 使いはじめた初日に「3日あきました」「連続を守りました」と出る

**症状**
インストール直後、記録がまだ1件もない状態のホームに

- 「3日あきました／数え直しになりますが…」
- 「🛡 今月は1日ぶん、連続を守りました」

が表示される。まだ1日も使っていない人が、いきなり「サボった」と言われる状態でした。

**原因**
連続日数の計算（`hr`）が今日から最大400日さかのぼるとき、**アプリを使いはじめる前の日**もトレーニング日として数えていた。
そのため、開始前の直近のトレーニング曜日で「1日ぶん保護」を使い切り、その前の日で連続が切れた判定になっていた。

**修正**
計算のさかのぼりを「そのユーザーが使いはじめた日」で止めるようにした。
開始日＝ 記録の最古の日付と同意（利用開始）日の、早いほう。

```
hr({ …, since: __ietoreStart(log, core) })   // 呼び出し側
for (…) { let k = dateKey(d); if (since && k < since) break; … }   // 集計ループ
```

初日は連続0日・保護0日・復帰メッセージなしになり、実際に間があいたときは今までどおり表示されます（既存の連続日数の数え方は変わりません）。

---

## 2. ホームの所要時間が「約1分」になる

**症状**
ととのえる日（有酸素20分＋ストレッチ）のホームに「5 種目 ／ **約1分**」と表示される。

**原因**
所要時間の見積り（`ir`）が「メイン」種目だけを合計していた。メインが無い日は合計0分→下限の1分になっていた。

**修正**
ホームのカードは**セッション全体**（ウォームアップ＋メイン＋有酸素＋クールダウン）で見積もるようにした。
トレーニング画面のほうは今までどおりメインだけの目安で、注記もそう書いてあるのでそのままです。

- ととのえる日：約1分 → **約20分**
- 上半身の日（30分設定・Lv.1）：**約42分**（トレーニング画面は「メインのめやす 約17分」）

---

## 3. 見出しの番号が ①③④ と飛ぶ

**症状**
ととのえる日のトレーニング画面が「① ウォームアップ → ③ 有酸素 → ④ クールダウン」となり、②が抜けて見える。
（メインが無い日は②が出ないため）
あわせて、はじめの質問の補足が「② メインの種目数が決まります。」と、いきなり番号から始まっていた。

**修正**
見出しから番号を外し、「ウォームアップ／メイン／有酸素／クールダウン」に統一。番号を参照していた2か所の文言も名前に置き換えた。

- トレーニング画面の注記 → 「メインのめやす 約◯分 ／ ウォームアップ・有酸素20分・クールダウンはこの時間の外です」
- はじめの質問の補足 → 「「メイン」の種目数が決まります。…」

---

## 4. 「すべての記録を消す」あとに画面が真っ白になる

**症状**
全消去 → 同意 → もう一度はじめの質問、と進んでもホームの中身が出ず、下のタブバーだけが残る。タブを押すと直る。

**原因**
全消去の処理で表示中のタブを `today` に切り替えていたが、タブのIDは `home / train / log / cal / mine` で `today` は存在しない。どの画面の条件にも当たらず、中身が描画されなかった。

**修正**
`home` に切り替えるようにした。（修正前後をブラウザで再現・確認済み）

---

## 5. 連続が途切れると、取ったバッジが消える

**症状**
「あつめたバッジ」の連続日数バッジが、連続が切れると 🔒 に戻る（85日→0日で「1か月つづいた」が消える）。
さらに、バッジ獲得の通知を閉じたときに保存済みの獲得段階が下がるため、同じバッジの通知が何度も出うる状態だった。

**原因**
表示も保存も、その時点の値（`Un()`）だけで計算していた。

**修正**
獲得段階は**下がらない**ようにした（保存済みと現在値の大きいほう）。
ホームの新規バッジ判定・記録タブの一覧・保存のすべてで同じ扱いにしています。次の段階までの残り回数は今までどおり現在値から計算します。

---

## 6. 「📤 ファイルに書き出す」がブラウザ版（Artifact）で何も起きない

**症状**
書き出しは `<a download>` で行っていて、Artifact のビューア内ではダウンロードが許可されないため無反応（成功メッセージだけ出る）。
バックアップはこのアプリで唯一の引き継ぎ手段なので、実質使えない状態でした。

**修正**
Artifact のダウンロード機能（`downloads`）を使って保存ダイアログを出すようにした。使えない環境では今までどおり `<a download>`、アプリ版（Capacitor）は共有シートのまま。
保存を断ったときは「書き出しをやめました。もう一度お試しください。」と出ます。

---

## 確認したこと

Chromium（390×844）で、初回起動 → 同意 → はじめの質問 → ホーム／トレーニング／記録／カレンダー／マイページ、
セット記録、連続モードのタイマー完走、体重の保存、曜日の入れ替え、全消去とやり直し、書き出し（成功・キャンセル・非対応環境）
を通しで実行。コンソールエラーは0件でした。

## 直していないこと（仕様と判断）

- ウォームアップやストレッチの秒数がレベルで伸びる（肩と腕まわし 40秒→1:30）のは、種目ごとに `cap` が設定されており意図的な設計と判断しました。
- 「Lv.」の数字が段階（stage）、絵文字が強さ（level）由来で別物なのも既存の設計のままにしています。
